events {}
http {
  include       mime.types;
  default_type  application/octet-stream;

  server {
    listen 3000;
    server_name mon-site.itsmenico.cc;

    root   /usr/share/nginx/html;
    index  index.html;

    location / {
      try_files $uri $uri/ /index.html;
    }
  }
}
